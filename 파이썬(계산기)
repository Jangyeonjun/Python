 = int(input("")) #수를 입력받는다
b = input()#연산자를 입력받는다
c = int(input(""))#수를입력받는다

if b == "+":#만약에 +이면 밑에있는 프린트문 실행
  print(a,"+",c,"=",a+c)
elif b == "-":#만약에+가아닌 -이면 밑에있는 프린트문 실행
   print(a,"-",c,"=",a-c)
elif b == "*":#만약에+,-가아닌* 이면 밑에있는 프린트문 실행
  print(a,"*",c,"=",a*c)
elif b == "/":#만약에+,-,*가아닌/ 이면 밑에있는 프린트문 실행
   print(a,"/",c,"=",a/c)
